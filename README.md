# Credit-Risk-ModeL
This is A Credit Risk Prediction model built using ML and Streamlit. Python libraries like NumPy,  Pandas, Matplotlib, Seaborn, and Scikit-learn were used for constructing this model.  It uses Classification algorithms to predict if a loan is likely to be passed or not and Streamlit is used to create a user interface. 
